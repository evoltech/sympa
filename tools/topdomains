#
# returns a report of the top domains of list subscribers.
# 

source ./common

mysql -s -u $DB_USER -p$DB_PASS --database=$DB_NAME --execute \
"select email_user from user_table" \
| sed 's/^.*@//' | sed 's/"//' | sort | uniq -c | sort -rn >> domains

